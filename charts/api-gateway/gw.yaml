_format_version: "3.0"
services:
- host: nr-vdyp-#{TARGET_ENV}#-backend.c50504-#{TARGET_ENV}#.svc
  name: nr-vdyp-#{TARGET_ENV}#-backend
  path: /
  plugins:
    - name: key-auth
      tags: [ "ns.#{GATEWAY_ID}#" ]
      protocols: [http, https]
      config:
        key_names: ["X-API-KEY"]
        run_on_preflight: true
        hide_credentials: true
        key_in_body: false
    - name: kong-upstream-jwt
      tags: [ "ns.#{GATEWAY_ID}#" ]
      config:
        header: GW-JWT
        include_credential_type: false
  port: 80
  protocol: http
  routes:
  - hosts:
    - vdyp#{ENV_URL_COMPONENT}#.api.gov.bc.ca
    methods:
    - GET
    name: vdyp-#{TARGET_ENV}#_~_get
    paths:
    - /api/v8
    plugins: []
    regex_priority: 200
    strip_path: false
    tags:
    - "ns.#{GATEWAY_ID}#"
  - hosts:
    - vdyp#{ENV_URL_COMPONENT}#.api.gov.bc.ca
    methods:
    - GET
    name: vdyp-#{TARGET_ENV}#_hcsv_get
    paths:
    - /api/v8/projection/hcsv
    plugins: []
    regex_priority: 200
    strip_path: false
    tags:
    - "ns.#{GATEWAY_ID}#"
  tags:
  - "ns.#{GATEWAY_ID}#"
upstreams: []

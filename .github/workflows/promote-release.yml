name: Promote Tagged Images and Deploy
run-name: Promote ${{ inputs.source_tag }} as ${{ inputs.release_tag }} to ${{ inputs.target_environment }}

on:
  workflow_dispatch:
    inputs:
      source_tag:
        description: Existing container tag to promote
        required: true
        type: string
      release_tag:
        description: Release tag to apply to the existing images
        required: true
        type: string
      chart_github_ref:
        description: Github Ref of the Chart to deploy
        required: false
        type: string
      target_environment:
        description: Target environment to deploy to
        required: true
        default: test
        type: choice
        options:
          - dev
          - test
          - prod

permissions:
  contents: read

concurrency:
  group: promote-${{ inputs.target_environment }}
  cancel-in-progress: false

jobs:
  tag-builds:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    strategy:
      matrix:
        component-name: [
          frontend, backend, batch
        ]
    steps:
      - uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN}}

      - name: Tag relevant images
        env:
          SOURCE_TAG: ${{ inputs.source_tag }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          docker pull ghcr.io/${{ github.repository }}/${{ matrix.component-name }}:$SOURCE_TAG
          docker tag ghcr.io/${{ github.repository }}/${{ matrix.component-name }}:$SOURCE_TAG ghcr.io/${{ github.repository }}/${{ matrix.component-name }}:$RELEASE_TAG
          docker push ghcr.io/${{ github.repository }}/${{ matrix.component-name }}:$RELEASE_TAG

  # include additional steps for DLV, PRD
  # rename to deploy-wftst, rename environment to tst
  deploy:
    needs: tag-builds
    uses: ./.github/workflows/openshift-target-environment.yml
    secrets: inherit
    with:
      target_environment: ${{ inputs.target_environment }}
      tag: ${{ inputs.release_tag }}
      ref: ${{ inputs.chart_github_ref }}
